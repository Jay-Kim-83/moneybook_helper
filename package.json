{
    "name": "moneybook",
    "version": "1.0.0",
    "description": "가계부 작성 도우미 (DATABASE_URL 있으면 Postgres, 없으면 파일 저장)",
    "main": "server.js",
    "scripts": {
        "start": "node server.js",
        "dev": "node --watch server.js"
    },
    "engines": {
        "node": ">=18"
    },
    "dependencies": {
        "pg": "^8.13.1"
    }
}
